<footer role="contentinfo" class="bg-gray-900 text-gray-300 mt-auto border-t border-gray-800">
  <div class="mx-auto max-w-7xl px-6 py-10 grid gap-8 md:grid-cols-3">
    <!-- Marque (renvoie vers Événements pour éviter la route manquante) -->
    <div>
      <a href="{{ path('event_index') }}" class="text-white text-lg font-semibold">GoRun</a>
      <p class="mt-3 text-sm text-gray-400">Courir, ensemble, partout.</p>
      <p class="mt-4 text-xs text-gray-500">© {{ 'now'|date('Y') }} GoRun. Tous droits réservés.</p>
    </div>

    <!-- Liens existants -->
    <nav aria-label="Découvrir" class="text-sm">
      <h3 class="text-white font-medium">Découvrir</h3>
      <ul class="mt-3 space-y-2">
        <li><a href="{{ path('event_index') }}" class="hover:text-white">Événements</a></li>
        <li><a href="{{ path('app_register') }}" class="hover:text-white">Inscription</a></li>
        <li><a href="{{ path('app_login') }}" class="hover:text-white">Connexion</a></li>
      </ul>
    </nav>

    <!-- Emplacements à venir (pas de path() tant que les routes n’existent pas) -->
    <nav aria-label="Support & Légal" class="text-sm">
      <h3 class="text-white font-medium">Support & Légal</h3>
      <ul class="mt-3 space-y-2">
        <li><span class="text-gray-500" title="Bientôt">Contact</span></li>
        <li><span class="text-gray-500" title="Bientôt">Mentions légales</span></li>
        <li><span class="text-gray-500" title="Bientôt">Politique de confidentialité</span></li>
        <li><span class="text-gray-500" title="Bientôt">Gestion des cookies</span></li>
        <li><span class="text-gray-500" title="Bientôt">Plan du site</span></li>
      </ul>
    </nav>
  </div>

  <div class="mx-auto max-w-7xl px-6 pb-8 flex flex-col md:flex-row items-center justify-between gap-4">
    <div class="flex items-center gap-4 text-sm">
      <a href="" target="_blank" rel="noopener" class="hover:text-white">Instagram</a>
      <a href="" target="_blank" rel="noopener" class="hover:text-white">Facebook</a>
    </div>
  </div>
</footer>
