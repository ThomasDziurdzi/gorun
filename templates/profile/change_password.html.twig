{% extends 'base.html.twig' %}

{% block title %}Changer mon mot de passe · GoRun{% endblock %}

{% block body %}
<div class="max-w-2xl mx-auto">
  
  <div class="flex items-center justify-between mb-6">
    <h1 class="text-2xl md:text-3xl font-bold">Changer mon mot de passe</h1>
    <a href="{{ path('app_profile') }}" class="text-green-600 hover:text-green-700 font-medium">
      ← Retour au profil
    </a>
  </div>

  {% for message in app.flashes('success') %}
    <div class="mb-6 bg-green-50 border border-green-200 text-green-800 px-6 py-4 rounded-2xl flex items-start gap-3">
      <svg class="w-5 h-5 mt-0.5 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
      </svg>
      <p>{{ message }}</p>
    </div>
  {% endfor %}

  {% for message in app.flashes('error') %}
    <div class="mb-6 bg-red-50 border border-red-200 text-red-800 px-6 py-4 rounded-2xl flex items-start gap-3">
      <svg class="w-5 h-5 mt-0.5 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"/>
      </svg>
      <p>{{ message }}</p>
    </div>
  {% endfor %}

  <div class="bg-white rounded-2xl shadow p-6">
    <p class="text-sm text-gray-600 mb-6">
      Pour des raisons de sécurité, vous devez entrer votre mot de passe actuel pour le modifier.
    </p>

    {{ form_start(form, {'attr': {'class': 'space-y-6'}}) }}

      <div>
        {{ form_label(form.currentPassword, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'}}) }}
        {{ form_widget(form.currentPassword) }}
        {{ form_errors(form.currentPassword) }}
      </div>

      <hr class="border-gray-200">

      <div>
        {{ form_label(form.newPassword.first, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'}}) }}
        {{ form_widget(form.newPassword.first) }}
        {{ form_errors(form.newPassword.first) }}
        <p class="mt-1 text-sm text-gray-500">
          ℹ️ Minimum 8 caractères. Utilisez un mélange de lettres, chiffres et caractères spéciaux.
        </p>
      </div>

      <div>
        {{ form_label(form.newPassword.second, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'}}) }}
        {{ form_widget(form.newPassword.second) }}
        {{ form_errors(form.newPassword.second) }}
      </div>

      {{ form_errors(form.newPassword) }}

      <div class="flex gap-4 pt-4">
        <button type="submit" class="bg-green-600 text-white px-6 py-3 rounded-2xl font-semibold hover:bg-green-700 transition">
          Changer le mot de passe
        </button>
        <a href="{{ path('app_profile') }}" class="bg-gray-200 text-gray-800 px-6 py-3 rounded-2xl font-semibold hover:bg-gray-300 transition">
          Annuler
        </a>
      </div>

    {{ form_end(form) }}
  </div>

  <div class="mt-6 bg-blue-50 border border-blue-200 rounded-2xl p-6">
    <h3 class="font-semibold text-blue-900 mb-3">Conseils pour un mot de passe sécurisé</h3>
    <ul class="text-sm text-blue-800 space-y-2">
      <li>Utilisez au moins 8 caractères</li>
      <li>Mélangez majuscules, minuscules, chiffres et symboles</li>
      <li>Évitez les mots du dictionnaire</li>
      <li>N'utilisez pas d'informations personnelles (date de naissance, etc.)</li>
      <li>Utilisez un mot de passe unique pour chaque site</li>
    </ul>
  </div>

</div>
{% endblock %}